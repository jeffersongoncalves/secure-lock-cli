name: Release

# Single linear release flow. The KEY invariant: the PHAR binary (composer `bin`,
# builds/secure-lock) is built and committed to main BEFORE the tag is created, and the
# tag is pinned to that exact commit. Nothing rebuilds onto a post-tag commit, so the
# tag never moves and Composer's stable-version immutability is never violated.

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Explicit version (e.g. 1.7.2). Leave blank to auto-bump the patch.'
        required: false

permissions: write-all

concurrency:
  group: release
  cancel-in-progress: false

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout main
        uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0

      - name: Resolve version
        id: v
        run: |
          if [ -n "${{ inputs.version }}" ]; then
            NEXT="${{ inputs.version }}"
            NEXT="${NEXT#v}"
          else
            LATEST=$(git tag -l \
              | sed 's/^v//' \
              | grep -E '^[0-9]+\.[0-9]+\.[0-9]+$' \
              | sort -V \
              | tail -n1)
            [ -z "$LATEST" ] && LATEST="0.0.0"
            IFS='.' read -r MA MI PA <<< "$LATEST"
            NEXT="${MA}.${MI}.$((PA + 1))"
          fi
          while gh release view "$NEXT" &>/dev/null; do
            IFS='.' read -r MA MI PA <<< "$NEXT"
            NEXT="${MA}.${MI}.$((PA + 1))"
          done
          echo "version=$NEXT" >> "$GITHUB_OUTPUT"
          echo "Releasing $NEXT"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: 8.4
          extensions: dom, curl, libxml, mbstring, zip
          ini-values: error_reporting=E_ALL, phar.readonly=Off
          tools: composer:v2
          coverage: none

      - name: Install the dependencies
        run: composer install --prefer-dist --optimize-autoloader --no-interaction --no-progress --no-suggest --no-scripts --ansi -v --ignore-platform-req=ext-pcntl --ignore-platform-req=ext-posix --ignore-platform-req=ext-sockets

      - name: Stamp embedded version
        run: echo "${{ steps.v.outputs.version }}" > version.txt

      - name: Update Changelog
        uses: stefanzweifel/changelog-updater-action@v1
        with:
          latest-version: ${{ steps.v.outputs.version }}
          release-notes: "Release ${{ steps.v.outputs.version }}"

      - name: Build the PHAR with the release version
        run: php secure-lock app:build secure-lock --build-version=${{ steps.v.outputs.version }}

      - name: Commit release artifacts to main
        id: commit
        uses: stefanzweifel/git-auto-commit-action@v7
        with:
          branch: main
          commit_message: "Release ${{ steps.v.outputs.version }}"
          file_pattern: "builds/secure-lock version.txt CHANGELOG.md"

      - name: Resolve release commit
        id: sha
        run: echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

      - name: Create tag and release at the release commit
        run: |
          gh release create ${{ steps.v.outputs.version }} \
            --title "${{ steps.v.outputs.version }}" \
            --notes "Release ${{ steps.v.outputs.version }}" \
            --target ${{ steps.sha.outputs.sha }} \
            --latest
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload the PHAR asset
        run: |
          cp builds/secure-lock secure-lock.phar
          gh release upload ${{ steps.v.outputs.version }} secure-lock.phar --clobber
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
